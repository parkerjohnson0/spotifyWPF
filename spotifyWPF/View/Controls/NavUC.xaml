<UserControl x:Class="spotifyWPF.View.Controls.NavUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:spotifyWPF.ViewModel"
             xmlns:conv="clr-namespace:spotifyWPF.ViewModel.Converters.Nav"
             xmlns:controls="clr-namespace:spotifyWPF.View.Controls"
             xmlns:local="clr-namespace:spotifyWPF.View.Controls"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="200">
    <UserControl.DataContext>
        <vm:NavVM/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <conv:ButtonConverter x:Key="ButtonConv"/>
    </UserControl.Resources>
    <Grid Background="Black">
        <StackPanel>
            
        <controls:NavItemUC 
            Background="Transparent"
            Command="{Binding HomeCommand}"
            FontSize="16"
            FontFamily="{StaticResource CircularMedium}"
            Text="Home"
            Cursor="Hand"
            VerticalAlignment="Top"
            Height="50">
            <controls:NavItemUC.Style>
                <Style TargetType="{x:Type controls:NavItemUC}">
                    <Setter Property="Image" Value="../Resources/Icons/home_unclicked.png"/>
                    <Setter Property="Foreground" Value="LightGray"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Image" Value="../Resources/Icons/home_unclicked_alt.png"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding Home.Active}" Value="True">
                            <Setter Property="Image" Value="../Resources/Icons/home_clicked.png"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </controls:NavItemUC.Style>
        </controls:NavItemUC>
        <controls:NavItemUC 
            Background="Transparent"
            FontSize="16"
            Command="{Binding HomeCommand}"
            
            FontFamily="{StaticResource CircularMedium}"
            Text="Search"
            Cursor="Hand"
            VerticalAlignment="Top"
            Height="50">
            <controls:NavItemUC.Style>
                <Style TargetType="{x:Type controls:NavItemUC}">
                    <Setter Property="Image" Value="../Resources/Icons/search_unclicked.png"/>
                    <Setter Property="Foreground" Value="LightGray"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Image" Value="../Resources/Icons/search_unclicked_alt.png"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding Path=Home.Active}" Value="True">
                            <Setter Property="Image" Value="../Resources/Icons/home_clicked.png"/>
                        </DataTrigger>
                        </Style.Triggers>
                    
                </Style>
            </controls:NavItemUC.Style>
        </controls:NavItemUC>
        <controls:NavItemUC 
            Background="Transparent"
            FontSize="16"
            Command="{Binding HomeCommand}"
            FontFamily="{StaticResource CircularMedium}"
            Text="Your Library"
            Cursor="Hand"
            VerticalAlignment="Top"
            Height="50">
            <controls:NavItemUC.Style>
                <Style TargetType="{x:Type controls:NavItemUC}">
                    <Setter Property="Image" Value="../Resources/Icons/library_unclicked.png"/>
                    <Setter Property="Foreground" Value="LightGray"/>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Image" Value="../Resources/Icons/library_unclicked_alt.png"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding Path=Home.Active}" Value="True">
                            <Setter Property="Image" Value="../Resources/Icons/home_clicked.png"/>
                        </DataTrigger>
                        </Style.Triggers>
                </Style>
            </controls:NavItemUC.Style>
        </controls:NavItemUC>
        </StackPanel>

        <!--<StackPanel>-->
            <!--<Grid>
                <Button HorizontalAlignment="Stretch">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Background="{TemplateBinding Background}">
                                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="TextBlock.Foreground" Value="LightGray"/>
                            <Setter Property="Image.Source" Value="../Resources/Icons/home_unclicked_alt.png"/>
                            --><!--<Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="TextBlock.Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>--><!--
                        </Style>
                    </Button.Style>
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <StackPanel.Style>
                            <Style>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorLevel=1,AncestorType=Button},Path=IsMouseOver}">
                                        <Setter Property="TextBlock.Foreground" Value="White"/>
                                        <Setter Property="Image.Source" Value="../Resources/Icons/home_unclicked.png"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <Image Margin="10,0"
                d:Source="../Resources/Icons/home_unclicked.png"
               Width="32" Height="32">
                            <Image.Style>
                                <Style TargetType="Image">
                                    <Setter Property="Source" Value="../Resources/Icons/home_unclicked_alt.png"/>
                                    <Style.Triggers>
                                        --><!--<Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Source" Value="../Resources/Icons/home_unclicked.png"/>
                                        </Trigger>--><!--
                                    </Style.Triggers>
                                </Style>
                            </Image.Style>
                        </Image>
                        <TextBlock 
                        FontSize="16"
                        Text="{Binding Home.Name}" 
                        VerticalAlignment="Center"
                        FontFamily="{StaticResource CircularMedium}"/>


                    </StackPanel>


                </Button>
            </Grid>

        </StackPanel>-->

    </Grid>
</UserControl>
